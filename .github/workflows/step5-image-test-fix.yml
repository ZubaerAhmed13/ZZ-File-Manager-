name: Step 5 one-shot image viewer test fix

on:
  push:
    branches: [ 'step5/removable-network-cloud' ]
    paths: [ '.github/workflows/step5-image-test-fix.yml' ]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: step5/removable-network-cloud
          fetch-depth: 0
      - name: Apply reviewed instrumentation patch
        run: python3 tools/step5_patch_image_viewer_test.py
      - name: Remove one-shot patch machinery and commit
        run: |
          rm tools/step5_patch_image_viewer_test.py .github/workflows/step5-image-test-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "test(android): stabilize image viewer foreground assertion"
          git push origin HEAD:step5/removable-network-cloud
